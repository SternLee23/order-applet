<!--pages/myOrder/myOrder.wxml-->
<view class="content">
    <view class="content_item" wx:key="*this" wx:for="{{orderList}}">
        <view>
            <view>{{item.foodName}}*{{item.num}}</view>
        </view>
    </view>
</view>
